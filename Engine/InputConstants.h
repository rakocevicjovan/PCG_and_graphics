#pragma once

///dropped this idea till I actually need it
enum class InputID
{
	MOUSE_L_PRESSED,
	MOUSE_L_RELEASED,

	MOUSE_R_PRESSED,
	MOUSE_R_RELEASED,

	MOUSE_M_PRESSED,
	MOUSE_M_RELEASED,
	MOUSE_M_ROLLED,

	KB_KEY_PRESSED,
	KB_KEY_RELEASED
};