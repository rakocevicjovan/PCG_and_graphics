#pragma once

// Rendering layer on a single render target. Example - opaque is base layer, transparent on top.
// Might not actually be used as a concrete class eventually but the concept exists. If it's accounted for otherwise, delete it.
class Layer
{

};