#pragma once

// Pass types. Hardcoded for now, not all of them have to be used. And more can be added if needed ofc.
enum PassType
{
	DEPTH_PREPASS,
	SHADOW,
	REFLECTION,
	BASE,
	SKYBOX,
	POSTPROCESS,
	GUI,
	DEBUG
};


struct RenderConfig
{
	z
};